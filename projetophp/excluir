<?php
include('Db.php')

if(!isset($_GET['id']) || empty($_GET['id'])){
    die("Nao encontrado");
}
$id = $_GET['id'];

$stmt = $conn->prepare("DELETE FROM alunos WHERE id = ?")
$stmt = $conn->bind_param

?>